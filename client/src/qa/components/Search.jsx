import React, { useState, useEffect } from 'react';

const Search = (props) => {
  // props
  // userSearch
  
  // methods
  // handleChange (sends value to App's userSearch)
  
};

export default Search;
